# from apscheduler.schedulers.background import BackgroundScheduler
# from datetime import datetime
# import requests

# scheduler = BackgroundScheduler()
# scheduler.add_job(
#     func=lambda: requests.get("http://127.0.0.1:8000/addappointments/"),
#     trigger='interval',
#     hours=1
# )
# scheduler.start()
